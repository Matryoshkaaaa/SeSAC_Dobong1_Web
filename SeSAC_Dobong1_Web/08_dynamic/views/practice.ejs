<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>axios get 회원가입</title>
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
  </head>
  <body>
    <form name="register">
      <label>
        이름
        <input type="text" name="name" />
      </label>
      <fieldset>
        <legend>성별</legend>
        <label><input type="radio" name="gender" value="남자" />남자</label>
        <label><input type="radio" name="gender" value="여자" />여자</label>
      </fieldset>
      <br />
      <fieldset>
        <legend>생년월일</legend>
        <select name="year" id="year">
          <% for (let i=1985; i<2003; i++){%>
          <option value="<%=i%>"><%=i%></option>

          <%}%>
        </select>
        <span>년</span>

        <select name="month" id="month">
          <% for (let i=1; i<=12; i++){%>
          <option value="<%=i%>"><%=i%></option>

          <%}%>
        </select>
        <span>월</span>
        <select name="date" id="date">
          <% for (let i=1; i<=31; i++){%>
          <option value="<%=i%>"><%=i%></option>

          <%}%>
        </select>
        <span>일</span>
      </fieldset>
      <br />
      <fieldset>
        <legend>관심사</legend>
        <label for="picnic"
          ><input type="checkbox" name="favorite" value="여행" id="picnic" />
          여행</label
        >
        <label for="fashion"
          ><input type="checkbox" name="favorite" value="패션" id="fashion" />
          패션</label
        >
        <label for="food"
          ><input type="checkbox" name="favorite" value="음식" id="food" />
          음식</label
        >
      </fieldset>
      <br />
      <button type="button" onclick="axiosGet()">회원가입</button>
    </form>
    <div class="result"></div>
  </body>
  <script>
    const resultBox = document.querySelector(".result");

    async function axiosGet() {
      const form = document.forms["register"];

      const favorite = [];

      const checked = document.querySelectorAll(
        'input[name="favorite"]:checked'
      );
      checked.forEach((checkbox) => {
        favorite.push(checkbox.value);
      });
      const data = {
        name: form.name.value,
        gender: form.gender.value,
        year: form.year.value,
        month: form.month.value,
        date: form.date.value,
        favorite: favorite,
      };
      try {
        const response = await axios({
          method: "GET",
          url: "/axios",
          params: data,

          // params: data, //get 일 때는 params 로 데이터 전달
        });
        console.log(response.data);
        const { name, gender, year, month, date, favorite } = response.data;

        resultBox.textContent = ` ${name}님의 성별은 ${gender}이고 생년월일은 ${year}년 ${month}월 ${date}일 이고 관심사는 ${favorite}입니다. `;
        console.log(
          ` ${name}님의 성별은 ${gender}이고 생년월일은 ${year}년 ${month}월 ${date}일 이고 관심사는 ${favorite}입니다. `
        );
      } catch (err) {
        console.log("err!!", err);
      }
    }
  </script>
</html>
